spring:
  datasource:
    url: jdbc:postgresql://ec2-3-36-105-86.ap-northeast-2.compute.amazonaws.com:5432/where42
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
